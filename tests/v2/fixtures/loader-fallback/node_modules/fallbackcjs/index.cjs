// CJS-only fixture: bare ESM `import('fallbackcjs')` cannot resolve this from core/src,
// so LibraryLoader must fall back to createRequire() from the provided resolveBase.
// Mimics a native (.node) lib that ESM import rejects but CJS require loads.
module.exports = { marker: 'loaded-via-createRequire-fallback' }
