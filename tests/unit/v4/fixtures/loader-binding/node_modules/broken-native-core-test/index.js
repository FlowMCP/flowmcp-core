// A module that RESOLVES (has a package.json) but THROWS at load — the canonical
// "native binding missing / ABI mismatch" failure shape. LibraryLoader.resolveExternal
// must classify this as INSTALLED-BUT-UNLOADABLE (LIB-BINDING), not NOT-INSTALLED (LIB-001).
throw new Error( "Could not locate the bindings file broken-native-core-test/build/Release/broken_native.node" )
